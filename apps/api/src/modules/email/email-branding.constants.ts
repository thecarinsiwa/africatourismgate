import type { EmailBrandingValue } from '@africatourismgate/types';

/** Aligné sur `DEFAULT_EMAIL_BRANDING` dans packages/types/src/organization-settings.ts */
export const DEFAULT_EMAIL_BRANDING: EmailBrandingValue = {
  displayName: 'Africa Tourism Gate',
  primaryColor: '#0d9488',
};
