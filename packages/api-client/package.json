{
  "name": "@africatourismgate/api-client",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "codegen": "pnpm -w codegen:api"
  },
  "dependencies": {
    "@africatourismgate/types": "workspace:*",
    "openapi-fetch": "^0.13.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
