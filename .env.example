# Database (MySQL)
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=
DATABASE_NAME=africatourismgate
# On API startup: import database/africatourismgate_database.sql if no tables exist (set false to disable)
DATABASE_AUTO_SCHEMA=true
# On API startup: import database/seeds/install.seed.sql if no users exist (set false to disable)
DATABASE_AUTO_SEED=true

# API (NestJS) — apps/api — http://localhost:3000/api
API_PORT=3000
API_GLOBAL_PREFIX=api

# Next.js apps (dev ports; used by pnpm dev scripts)
ADMIN_PORT=3001
WEB_PORT=3002
POS_PORT=3003

# Public API URL for Next.js apps (browser + server)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Admin app URL (dev: localhost; production: https://app-africatourismgate.org)
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001

# CORS — required when running all frontends with pnpm dev
CORS_ORIGIN=http://localhost:3001,http://localhost:3002,http://localhost:3003

JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
